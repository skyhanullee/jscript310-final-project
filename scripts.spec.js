describe('Main search', () => {
    let recipeName = '';
    const url = `https://www.themealdb.com/api/json/v1/1/search.php?s${recipeName}`;
    
    describe('fetchRecipe function', () => {
        it('should fetch curry recipes', () => {
            recipeName = 'curry';
            expect()
        });
    });

});